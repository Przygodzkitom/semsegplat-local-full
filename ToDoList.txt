-- Remove excessive buttons, option etc from GUI
-- Inference module must include feature to batch analyse iamges and calculate numbers of objects in each classes (other object features? size etc.)-export results to csv?
-- Model training for brush annotations is downsized so taht it coulb tested on CPU, must be upgraded to ResNet100 for production
-- Training fails when user adds new images and annotations and retrains the model - corrected, but check again
-- Sometimes uploading of new images failes if there are already some uploaded from the start, it needs to refresh streamlit to the upload work again
-- training failed on brush annotated images, but it may be due to low calculation capacity - check on GPU

